name: tasks_api
description: The interface and models for an API providing access to tasks.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  equatable: ^2.0.3
  json_annotation: ^4.9.0
  meta: ^1.7.0
  uuid: ^3.0.6
  category_api:
    path: ../category_api

dev_dependencies:
  build_runner: ^2.2.0
  mocktail: ^1.0.4
  test: ^1.25.8
  json_serializable: ^6.3.1
  very_good_analysis: ^6.0.0
